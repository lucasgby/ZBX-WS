import { Request, Response } from "express";
//import { client } from "../model/clientWS";

const connectQrCode = async (req: Request, res: Response) => {
  try {
    
  } catch (error) {
    
  }
}

export {
  connectQrCode
}